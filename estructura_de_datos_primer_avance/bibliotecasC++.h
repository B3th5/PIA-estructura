#pragma once // Prevenir la inclusión múltiple del archivo

// Librerías del sistema
#include <Windows.h>  // Librería principal de Windows API
#include <CommCtrl.h> // Controles comunes de Windows
#include <CommDlg.h>  // Diálogos comunes de Windows

// Librerías de C++
#include <iostream>   // Entrada y salida estándar
#include <fstream>    // Manejo de archivos
#include <ctime>      // Funciones de tiempo
#include <string>     // Librería de cadenas de caracteres en C++
#include <cstring>    // Funciones de manejo de cadenas estilo C
#include<vector>	// Contenedor de la biblioteca estándar de C++ para manejar arrays dinámicos
#include <cmath> // Incluye esto en la parte superior de tu archivo si aún no está
#include <algorithm> // Para std::sort

// Recursos propios del proyecto
#include "resource.h" // Archivo de recursos, típicamente definiciones de menús, iconos, etc.

// Espacio de nombres
using namespace std; // Facilita la escritura del código evitando std:: repetidamente